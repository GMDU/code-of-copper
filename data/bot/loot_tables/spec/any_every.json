{
  "pools": [
    {
      "rolls": 1,
      "entries": [
        {
          "type": "minecraft:item",
          "name": "minecraft:writable_book",
          "functions": [
            {
              "function": "minecraft:set_nbt",
              "tag": "{pages:['print(\">>> EVERY/ANY\")\nprint(\"Expected output:\")\nprint(false)\nprint(true)\nprint(true)\nprint(false)\nprint(6)\n\nprint(\"---\")\n\nprint(any([], ->()true))\nprint(every([], ->()false))','print(any([1,2,3,4,5], -> (x) x % 2 == 0 && x >= 4))\nprint(every([2,4,6,7], -> () x % 2 == 0))\nlet last = 0\nevery([1,3,5,6], -> (x) {last = x\nreturn x % 2 != 0\n}) \nprint(last)\n\nprint(\">>>\")']}"
            }
          ]
        }
      ]
    }
  ]
}